using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class AudioHelpers
{
    //public static float[] spectrum = new float[64];

    //public static float[] ApplyAudioSourceFrequency(float[] spectrum)
    //{
    //    mainTrack.GetSpectrumData(spectrum, 0, FFTWindow.Triangle);

    //    for (int i = 0; i < createdObjects.Length; i++)
    //    {
    //        var specVal = _spectrum[i];
    //        var go = createdObjects[i];

    //        go.transform.DOShakeScale(durationToSample, Vector3.up * specVal * punchIntensity);
    //    }
    //}
}
